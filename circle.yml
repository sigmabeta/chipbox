dependencies:
  pre:
    - echo y | android update sdk --no-ui --all --filter build-tools-23.0.3

test:
  override:
    - ./gradlew assembleArm_64Debug -PdisablePreDex
    - ./gradlew assembleX86_64Debug -PdisablePreDex