<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application>
        <provider
            android:name="net.sigmabeta.chipbox.artworkprovider.api.ArtworkProvider"
            android:authorities="${applicationId}.artworkprovider.api"
            android:exported="true"
            android:grantUriPermissions="true" />
    </application>
</manifest>
